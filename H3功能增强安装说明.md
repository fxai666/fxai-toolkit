# MiniMax H3 功能增强安装说明

本补丁修改了 ComfyUI 核心的两个 Python 文件，为 MiniMax H3 增加：

1. **外置音频口型驱动**：支持传入参考音频驱动口型（Ref2VA 模型）
2. **首尾帧锚定增强**：首尾帧作为关键帧几何锚定在潜空间两端
3. **参考图 + 参考音频混合**：支持图片列表（多张参考图）+ 单个音频同时注入

## 安装步骤

### 1. 找到 ComfyUI 安装目录

本说明中 `<ComfyUI目录>` 指你安装 ComfyUI 的根目录（包含 `main.py`、`comfy`、`custom_nodes` 等文件夹的那个目录）。

### 2. 备份原文件（强烈建议）

分别把以下两个原文件复制一份备份，方便以后还原：

- `<ComfyUI目录>\comfy\ldm\minimax\model.py`
- `<ComfyUI目录>\comfy\model_base.py`

### 3. 拷贝修改后的文件到对应目录

把下载得到的两个文件分别覆盖到对应的目录：

| 修改后的文件 | 拷贝到（相对 ComfyUI 根目录） |
|---|---|
| `model.py` | `comfy\ldm\minimax\model.py` |
| `model_base.py` | `comfy\model_base.py` |

即：

- `model.py` 放到 `<ComfyUI目录>\comfy\ldm\minimax\` 下
- `model_base.py` 放到 `<ComfyUI目录>\comfy\` 下

### 4. 重启 ComfyUI

覆盖完成后重启 ComfyUI，即可在界面上使用凤希AI的 MiniMax H3 相关节点（图片列表参考 + 外置音频）。

## 常见问题

**Q：覆盖后报错 / 节点不生效？**
A：确认两个文件都放对了目录，且文件名没有变成 `model(1).py` 之类。重启后再试。

**Q：口型还是不动？**
A：外置音频口型需要 **Ref2VA** 模型（`minimax_h3_ref2va_pruned_int8_convrot.safetensors` 等），FL2VA 模型不支持音频条件。

**Q：以后想还原？**
A：用第 2 步备份的原文件覆盖回去即可。

## 涉及版本

针对 ComfyUI v0.30.1（MiniMax H3 官方支持版本）测试通过。
